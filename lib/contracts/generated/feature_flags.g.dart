// GENERATED by tool/contracts/generate.mjs from contracts/feature-flags.v1.json — DO NOT EDIT. sha256:e650e474b673
// dart format off
// contract feature-flags v1 revision 1. Regenerate with: node tool/contracts/generate.mjs

/// `appConfig/features` keys in contract order (ADR-010, V1-05 §4.17). `wire` is the Firestore field name.
enum FeatureFlagKey {
  /// Phase existing.
  gut('gut', defaultValue: false),
  /// Phase existing.
  blood('blood', defaultValue: false),
  /// Phase existing.
  insights('insights', defaultValue: false),
  /// Phase P1a.
  soapV2('soapV2', defaultValue: false),
  /// Phase P1a.
  bodyComposition('bodyComposition', defaultValue: false),
  /// Phase P1b.
  bodyAssessment('bodyAssessment', defaultValue: false),
  /// Phase P2.
  memberShare('memberShare', defaultValue: false),
  /// Phase P2.
  lidarBeta('lidarBeta', defaultValue: false),
  ;

  const FeatureFlagKey(this.wire, {required this.defaultValue});

  /// The field name in `appConfig/features` and in contracts/feature-flags.v1.json.
  final String wire;

  /// Value when the document or the key is missing, or the stored value is not a bool (AC-IA-02).
  final bool defaultValue;

  /// Returns null for a missing or unknown key.
  static FeatureFlagKey? fromWire(String? value) {
    for (final candidate in FeatureFlagKey.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}
