// GENERATED by tool/contracts/generate.mjs from contracts/vocab.v1.json — DO NOT EDIT. sha256:08e4e94d5b0c
// dart format off
// contract vocab v1 revision 1. Regenerate with: node tool/contracts/generate.mjs

/// Whether a vocab block is confirmed or still a draft (V1-05 §13.2).
enum VocabStatus { confirmed, draft }

/// vocab `sourceGrade` (confirmed).
enum SourceGrade {
  tape('tape'),
  device('device'),
  photoManual('photoManual'),
  photoAuto('photoAuto'),
  observedSection('observedSection'),
  modelEstimate('modelEstimate'),
  aiAppearance('aiAppearance'),
  selfReport('selfReport'),
  trainerObserved('trainerObserved'),
  derived('derived'),
  ;

  const SourceGrade(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SourceGrade? fromWire(String? value) {
    for (final candidate in SourceGrade.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `reliabilityTier` (confirmed).
enum ReliabilityTier {
  tier1('tier1'),
  reference('reference'),
  beta('beta'),
  ;

  const ReliabilityTier(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ReliabilityTier? fromWire(String? value) {
    for (final candidate in ReliabilityTier.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `changeStatus` (confirmed).
enum ChangeStatus {
  meaningfulImprovement('meaningfulImprovement'),
  withinError('withinError'),
  meaningfulDecline('meaningfulDecline'),
  indeterminate('indeterminate'),
  pendingPolicy('pendingPolicy'),
  ;

  const ChangeStatus(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ChangeStatus? fromWire(String? value) {
    for (final candidate in ChangeStatus.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `reasonCode` (confirmed).
enum ReasonCode {
  conditionMismatch('conditionMismatch'),
  deviceChanged('deviceChanged'),
  protocolChanged('protocolChanged'),
  noMdc('noMdc'),
  noComparison('noComparison'),
  referenceMetric('referenceMetric'),
  betaMetric('betaMetric'),
  ;

  const ReasonCode(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ReasonCode? fromWire(String? value) {
    for (final candidate in ReasonCode.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `mdcSource` (confirmed).
enum MdcSource {
  literature('literature'),
  inHouse('inHouse'),
  ;

  const MdcSource(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static MdcSource? fromWire(String? value) {
    for (final candidate in MdcSource.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `improvementDirection` (confirmed).
enum ImprovementDirection {
  higherIsBetter('higherIsBetter'),
  lowerIsBetter('lowerIsBetter'),
  towardZero('towardZero'),
  none('none'),
  ;

  const ImprovementDirection(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ImprovementDirection? fromWire(String? value) {
    for (final candidate in ImprovementDirection.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `side` (confirmed).
enum Side {
  none('none'),
  left('left'),
  right('right'),
  bilateral('bilateral'),
  ;

  const Side(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Side? fromWire(String? value) {
    for (final candidate in Side.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `syncState` (confirmed).
enum SyncState {
  localSaved('localSaved'),
  syncing('syncing'),
  synced('synced'),
  syncFailed('syncFailed'),
  awaitingConsent('awaitingConsent'),
  ;

  const SyncState(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SyncState? fromWire(String? value) {
    for (final candidate in SyncState.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `family` (confirmed).
enum Family {
  posture('posture'),
  bodyComposition('bodyComposition'),
  circumference('circumference'),
  pain('pain'),
  trainerObservation('trainerObservation'),
  ;

  const Family(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Family? fromWire(String? value) {
    for (final candidate in Family.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `sideRule` (confirmed).
enum SideRule {
  none('none'),
  leftRight('leftRight'),
  leftRightBilateral('leftRightBilateral'),
  magnitudeWithLowerSide('magnitudeWithLowerSide'),
  magnitudeWithHigherSide('magnitudeWithHigherSide'),
  undefinedV2('undefinedV2'),
  ;

  const SideRule(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SideRule? fromWire(String? value) {
    for (final candidate in SideRule.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `availability` (confirmed).
enum Availability {
  v1('v1'),
  v1Reference('v1Reference'),
  v1ReferenceDefaultOff('v1ReferenceDefaultOff'),
  v1Optional('v1Optional'),
  v2('v2'),
  ;

  const Availability(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Availability? fromWire(String? value) {
    for (final candidate in Availability.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `scale` (confirmed).
enum Scale {
  ratio('ratio'),
  ordinal('ordinal'),
  ;

  const Scale(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Scale? fromWire(String? value) {
    for (final candidate in Scale.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `unit` (confirmed).
enum MetricUnit {
  deg('deg'),
  kg('kg'),
  percent('percent'),
  kgPerM2('kgPerM2'),
  level('level'),
  liter('liter'),
  cm('cm'),
  point('point'),
  grade('grade'),
  ;

  const MetricUnit(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static MetricUnit? fromWire(String? value) {
    for (final candidate in MetricUnit.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `consentType` (confirmed).
enum ConsentType {
  required('required'),
  healthData('healthData'),
  bodyImaging('bodyImaging'),
  sharing('sharing'),
  research('research'),
  ;

  const ConsentType(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ConsentType? fromWire(String? value) {
    for (final candidate in ConsentType.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `consentAction` (confirmed).
enum ConsentAction {
  grant('grant'),
  withdraw('withdraw'),
  ;

  const ConsentAction(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ConsentAction? fromWire(String? value) {
    for (final candidate in ConsentAction.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `consentChannel` (confirmed).
enum ConsentChannel {
  memberApp('memberApp'),
  trainerDeviceInPerson('trainerDeviceInPerson'),
  ;

  const ConsentChannel(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ConsentChannel? fromWire(String? value) {
    for (final candidate in ConsentChannel.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `soapStatus` (confirmed).
enum SoapStatus {
  draft('draft'),
  finalized('finalized'),
  ;

  const SoapStatus(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SoapStatus? fromWire(String? value) {
    for (final candidate in SoapStatus.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `postureStatus` (confirmed).
enum PostureStatus {
  draft('draft'),
  confirmed('confirmed'),
  voided('voided'),
  ;

  const PostureStatus(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static PostureStatus? fromWire(String? value) {
    for (final candidate in PostureStatus.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `measurementStatus` (confirmed).
enum MeasurementStatus {
  active('active'),
  voided('voided'),
  ;

  const MeasurementStatus(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static MeasurementStatus? fromWire(String? value) {
    for (final candidate in MeasurementStatus.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `pendingMemberStatus` (confirmed).
enum PendingMemberStatus {
  pending('pending'),
  promoted('promoted'),
  cancelled('cancelled'),
  expired('expired'),
  ;

  const PendingMemberStatus(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static PendingMemberStatus? fromWire(String? value) {
    for (final candidate in PendingMemberStatus.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `summaryStatus` (confirmed).
enum SummaryStatus {
  shared('shared'),
  revoked('revoked'),
  ;

  const SummaryStatus(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SummaryStatus? fromWire(String? value) {
    for (final candidate in SummaryStatus.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `postureView` (confirmed).
enum PostureView {
  front('front'),
  sagittalLeft('sagittalLeft'),
  sagittalRight('sagittalRight'),
  ;

  const PostureView(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static PostureView? fromWire(String? value) {
    for (final candidate in PostureView.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `clothing` (draft, confirmBy DF-915).
enum Clothing {
  fitted('fitted'),
  regular('regular'),
  unknown('unknown'),
  ;

  const Clothing(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Clothing? fromWire(String? value) {
    for (final candidate in Clothing.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `fasting` (confirmed).
enum Fasting {
  yes('yes'),
  no('no'),
  unknown('unknown'),
  ;

  const Fasting(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Fasting? fromWire(String? value) {
    for (final candidate in Fasting.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `timeOfDayBand` (confirmed).
enum TimeOfDayBand {
  morning('morning'),
  midday('midday'),
  evening('evening'),
  ;

  const TimeOfDayBand(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static TimeOfDayBand? fromWire(String? value) {
    for (final candidate in TimeOfDayBand.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `bodyCompositionSource` (confirmed).
enum BodyCompositionSource {
  manualEntry('manualEntry'),
  inbodyApi('inbodyApi'),
  healthKit('healthKit'),
  ;

  const BodyCompositionSource(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static BodyCompositionSource? fromWire(String? value) {
    for (final candidate in BodyCompositionSource.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `circumferenceProtocolId` (confirmed).
enum CircumferenceProtocolId {
  waistMidpoint('waistMidpoint'),
  hipMaximum('hipMaximum'),
  custom('custom'),
  ;

  const CircumferenceProtocolId(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static CircumferenceProtocolId? fromWire(String? value) {
    for (final candidate in CircumferenceProtocolId.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `landmarkCode` (confirmed).
enum LandmarkCode {
  tragusLeft('tragusLeft'),
  tragusRight('tragusRight'),
  c7('c7'),
  earLeft('earLeft'),
  earRight('earRight'),
  acromionLeft('acromionLeft'),
  acromionRight('acromionRight'),
  asisLeft('asisLeft'),
  asisRight('asisRight'),
  ;

  const LandmarkCode(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static LandmarkCode? fromWire(String? value) {
    for (final candidate in LandmarkCode.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `activeOrPassive` (draft, confirmBy DF-916).
enum ActiveOrPassive {
  active('active'),
  passive('passive'),
  ;

  const ActiveOrPassive(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ActiveOrPassive? fromWire(String? value) {
    for (final candidate in ActiveOrPassive.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `joint` (draft, confirmBy DF-916).
enum Joint {
  cervicalSpine('cervicalSpine'),
  thoracicSpine('thoracicSpine'),
  lumbarSpine('lumbarSpine'),
  shoulder('shoulder'),
  elbow('elbow'),
  wrist('wrist'),
  hip('hip'),
  knee('knee'),
  ankle('ankle'),
  ;

  const Joint(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Joint? fromWire(String? value) {
    for (final candidate in Joint.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `motion` (draft, confirmBy DF-916).
enum Motion {
  flexion('flexion'),
  extension('extension'),
  abduction('abduction'),
  adduction('adduction'),
  horizontalAbduction('horizontalAbduction'),
  horizontalAdduction('horizontalAdduction'),
  internalRotation('internalRotation'),
  externalRotation('externalRotation'),
  lateralFlexion('lateralFlexion'),
  rotation('rotation'),
  dorsiflexion('dorsiflexion'),
  plantarflexion('plantarflexion'),
  inversion('inversion'),
  eversion('eversion'),
  ;

  const Motion(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Motion? fromWire(String? value) {
    for (final candidate in Motion.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `muscleGroup` (draft, confirmBy DF-916).
enum MuscleGroup {
  neckFlexors('neckFlexors'),
  neckExtensors('neckExtensors'),
  shoulderFlexors('shoulderFlexors'),
  shoulderAbductors('shoulderAbductors'),
  shoulderInternalRotators('shoulderInternalRotators'),
  shoulderExternalRotators('shoulderExternalRotators'),
  elbowFlexors('elbowFlexors'),
  elbowExtensors('elbowExtensors'),
  trunkFlexors('trunkFlexors'),
  trunkExtensors('trunkExtensors'),
  hipFlexors('hipFlexors'),
  hipExtensors('hipExtensors'),
  hipAbductors('hipAbductors'),
  hipAdductors('hipAdductors'),
  kneeExtensors('kneeExtensors'),
  kneeFlexors('kneeFlexors'),
  ankleDorsiflexors('ankleDorsiflexors'),
  anklePlantarflexors('anklePlantarflexors'),
  ;

  const MuscleGroup(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static MuscleGroup? fromWire(String? value) {
    for (final candidate in MuscleGroup.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `regionCode` (draft, confirmBy DF-916).
enum RegionCode {
  head('head'),
  neck('neck'),
  shoulderLeft('shoulderLeft'),
  shoulderRight('shoulderRight'),
  upperBack('upperBack'),
  lowerBack('lowerBack'),
  chest('chest'),
  abdomen('abdomen'),
  elbowLeft('elbowLeft'),
  elbowRight('elbowRight'),
  wristHandLeft('wristHandLeft'),
  wristHandRight('wristHandRight'),
  hipLeft('hipLeft'),
  hipRight('hipRight'),
  thighLeft('thighLeft'),
  thighRight('thighRight'),
  kneeLeft('kneeLeft'),
  kneeRight('kneeRight'),
  calfLeft('calfLeft'),
  calfRight('calfRight'),
  ankleFootLeft('ankleFootLeft'),
  ankleFootRight('ankleFootRight'),
  ;

  const RegionCode(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static RegionCode? fromWire(String? value) {
    for (final candidate in RegionCode.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// Allowed motions for one joint (vocab `jointMotionPairs`).
class JointMotionPair {
  const JointMotionPair({required this.joint, required this.motions});

  final Joint joint;
  final List<Motion> motions;
}

/// vocab `jointMotionPairs` (draft, confirmBy DF-916). While draft the list may be empty and codecs do not check pairs (ASM-P0-04).
abstract final class JointMotionPairs {
  static const VocabStatus vocabStatus = VocabStatus.draft;
  static const List<JointMotionPair> pairs = [];
}
