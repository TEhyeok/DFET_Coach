{
  "description": "a flag key must not become the generated defaultValue member after Dart escaping (default -> defaultValue)",
  "file": "feature-flags.v1.json",
  "patch": [
    {
      "op": "add",
      "path": "/flags/-",
      "value": {
        "key": "default",
        "default": false,
        "phase": "P3",
        "descriptionKo": "테스트",
        "ruleGatedCollections": []
      }
    }
  ],
  "expect": [
    "contracts/feature-flags.v1.json#/flags/8/key: \"default\" collides with the generated FeatureFlagKey member defaultValue"
  ]
}
