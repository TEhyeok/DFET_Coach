// GENERATED by tool/contracts/generate.mjs from contracts/vocab.v1.json — DO NOT EDIT. sha256:79b3aeceee8d
// dart format off
// contract vocab v1 revision 7. Regenerate with: node tool/contracts/generate.mjs

/// Whether a vocab block is confirmed or still a draft (V1-05 §13.2).
enum VocabStatus { confirmed, draft }

/// vocab `sourceGrade` (confirmed).
enum SourceGrade {
  tape('tape'),
  photoAuto('photoAuto'),
  derived('derived'),
  ;

  const SourceGrade(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SourceGrade? fromWire(String? value) {
    for (final candidate in SourceGrade.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `reliabilityTier` (confirmed).
enum ReliabilityTier {
  tier1('tier1'),
  reference('reference'),
  ;

  const ReliabilityTier(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ReliabilityTier? fromWire(String? value) {
    for (final candidate in ReliabilityTier.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `improvementDirection` (confirmed).
enum ImprovementDirection {
  higherIsBetter('higherIsBetter'),
  none('none'),
  ;

  const ImprovementDirection(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static ImprovementDirection? fromWire(String? value) {
    for (final candidate in ImprovementDirection.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `family` (confirmed).
enum Family {
  posture('posture'),
  bodyComposition('bodyComposition'),
  ;

  const Family(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Family? fromWire(String? value) {
    for (final candidate in Family.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `sideRule` (confirmed).
enum SideRule {
  none('none'),
  leftRight('leftRight'),
  ;

  const SideRule(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static SideRule? fromWire(String? value) {
    for (final candidate in SideRule.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `availability` (confirmed).
enum Availability {
  v1('v1'),
  v2('v2'),
  ;

  const Availability(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Availability? fromWire(String? value) {
    for (final candidate in Availability.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `scale` (confirmed).
enum Scale {
  ratio('ratio'),
  ;

  const Scale(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Scale? fromWire(String? value) {
    for (final candidate in Scale.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `unit` (confirmed).
enum MetricUnit {
  deg('deg'),
  kg('kg'),
  ;

  const MetricUnit(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.confirmed;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static MetricUnit? fromWire(String? value) {
    for (final candidate in MetricUnit.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `keywordish` (draft, confirmBy DF-999).
enum Keywordish {
  defaultValue('default'),
  required('required'),
  valuesValue('values'),
  inValue('in'),
  self('self'),
  ;

  const Keywordish(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Keywordish? fromWire(String? value) {
    for (final candidate in Keywordish.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `joint` (draft, confirmBy DF-916).
enum Joint {
  knee('knee'),
  ;

  const Joint(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Joint? fromWire(String? value) {
    for (final candidate in Joint.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// vocab `motion` (draft, confirmBy DF-916).
enum Motion {
  flexion('flexion'),
  extension('extension'),
  ;

  const Motion(this.wire);

  /// The string stored in Firestore and in contracts/*.json.
  final String wire;

  static const VocabStatus vocabStatus = VocabStatus.draft;

  /// Returns null for a missing or unknown value. Callers keep the raw string (해석 불가).
  static Motion? fromWire(String? value) {
    for (final candidate in Motion.values) {
      if (candidate.wire == value) return candidate;
    }
    return null;
  }
}

/// Allowed motions for one joint (vocab `jointMotionPairs`).
class JointMotionPair {
  const JointMotionPair({required this.joint, required this.motions});

  final Joint joint;
  final List<Motion> motions;
}

/// vocab `jointMotionPairs` (draft, confirmBy DF-916). While draft the list may be empty and codecs do not check pairs (ASM-P0-04).
abstract final class JointMotionPairs {
  static const VocabStatus vocabStatus = VocabStatus.draft;
  static const List<JointMotionPair> pairs = [
    JointMotionPair(
      joint: Joint.knee,
      motions: [
        Motion.flexion,
        Motion.extension,
      ],
    ),
  ];
}
