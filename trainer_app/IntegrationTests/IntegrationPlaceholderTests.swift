import XCTest

/// Placeholder so the DFETTrainerIntegrationTests bundle exists. Emulator-backed integration tests
/// (demo-dfet project) arrive with DF-107; CI skips this bundle until then.
final class IntegrationPlaceholderTests: XCTestCase {
  func testPlaceholder() {
    XCTAssertTrue(true)
  }
}
