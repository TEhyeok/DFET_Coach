// GENERATED by tool/contracts/generate.mjs from contracts/vocab.v1.json — DO NOT EDIT. sha256:08e4e94d5b0c
// contract vocab v1 revision 1. Regenerate with: node tool/contracts/generate.mjs

/// Whether a vocab block is confirmed or still a draft (V1-05 §13.2).
public enum VocabStatus: String, Codable, CaseIterable, Sendable {
  case confirmed
  case draft
}

/// vocab `sourceGrade` (confirmed).
public enum SourceGrade: String, Codable, CaseIterable, Sendable {
  case tape
  case device
  case photoManual
  case photoAuto
  case observedSection
  case modelEstimate
  case aiAppearance
  case selfReport
  case trainerObserved
  case derived

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `reliabilityTier` (confirmed).
public enum ReliabilityTier: String, Codable, CaseIterable, Sendable {
  case tier1
  case reference
  case beta

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `changeStatus` (confirmed).
public enum ChangeStatus: String, Codable, CaseIterable, Sendable {
  case meaningfulImprovement
  case withinError
  case meaningfulDecline
  case indeterminate
  case pendingPolicy

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `reasonCode` (confirmed).
public enum ReasonCode: String, Codable, CaseIterable, Sendable {
  case conditionMismatch
  case deviceChanged
  case protocolChanged
  case noMdc
  case noComparison
  case referenceMetric
  case betaMetric

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `mdcSource` (confirmed).
public enum MdcSource: String, Codable, CaseIterable, Sendable {
  case literature
  case inHouse

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `improvementDirection` (confirmed).
public enum ImprovementDirection: String, Codable, CaseIterable, Sendable {
  case higherIsBetter
  case lowerIsBetter
  case towardZero
  case `none` = "none"

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `side` (confirmed).
public enum Side: String, Codable, CaseIterable, Sendable {
  case `none` = "none"
  case left
  case right
  case bilateral

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `syncState` (confirmed).
public enum SyncState: String, Codable, CaseIterable, Sendable {
  case localSaved
  case syncing
  case synced
  case syncFailed
  case awaitingConsent

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `family` (confirmed).
public enum Family: String, Codable, CaseIterable, Sendable {
  case posture
  case bodyComposition
  case circumference
  case pain
  case trainerObservation

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `sideRule` (confirmed).
public enum SideRule: String, Codable, CaseIterable, Sendable {
  case `none` = "none"
  case leftRight
  case leftRightBilateral
  case magnitudeWithLowerSide
  case magnitudeWithHigherSide
  case undefinedV2

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `availability` (confirmed).
public enum Availability: String, Codable, CaseIterable, Sendable {
  case v1
  case v1Reference
  case v1ReferenceDefaultOff
  case v1Optional
  case v2

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `scale` (confirmed).
public enum Scale: String, Codable, CaseIterable, Sendable {
  case ratio
  case ordinal

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `unit` (confirmed).
public enum MetricUnit: String, Codable, CaseIterable, Sendable {
  case deg
  case kg
  case percent
  case kgPerM2
  case level
  case liter
  case cm
  case point
  case grade

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `consentType` (confirmed).
public enum ConsentType: String, Codable, CaseIterable, Sendable {
  case required
  case healthData
  case bodyImaging
  case sharing
  case research

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `consentAction` (confirmed).
public enum ConsentAction: String, Codable, CaseIterable, Sendable {
  case grant
  case withdraw

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `consentChannel` (confirmed).
public enum ConsentChannel: String, Codable, CaseIterable, Sendable {
  case memberApp
  case trainerDeviceInPerson

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `soapStatus` (confirmed).
public enum SoapStatus: String, Codable, CaseIterable, Sendable {
  case draft
  case finalized

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `postureStatus` (confirmed).
public enum PostureStatus: String, Codable, CaseIterable, Sendable {
  case draft
  case confirmed
  case voided

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `measurementStatus` (confirmed).
public enum MeasurementStatus: String, Codable, CaseIterable, Sendable {
  case active
  case voided

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `pendingMemberStatus` (confirmed).
public enum PendingMemberStatus: String, Codable, CaseIterable, Sendable {
  case pending
  case promoted
  case cancelled
  case expired

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `summaryStatus` (confirmed).
public enum SummaryStatus: String, Codable, CaseIterable, Sendable {
  case shared
  case revoked

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `postureView` (confirmed).
public enum PostureView: String, Codable, CaseIterable, Sendable {
  case front
  case sagittalLeft
  case sagittalRight

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `clothing` (draft, confirmBy DF-915).
public enum Clothing: String, Codable, CaseIterable, Sendable {
  case fitted
  case regular
  case unknown

  public static let vocabStatus: VocabStatus = .draft
}

/// vocab `fasting` (confirmed).
public enum Fasting: String, Codable, CaseIterable, Sendable {
  case yes
  case no
  case unknown

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `timeOfDayBand` (confirmed).
public enum TimeOfDayBand: String, Codable, CaseIterable, Sendable {
  case morning
  case midday
  case evening

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `bodyCompositionSource` (confirmed).
public enum BodyCompositionSource: String, Codable, CaseIterable, Sendable {
  case manualEntry
  case inbodyApi
  case healthKit

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `circumferenceProtocolId` (confirmed).
public enum CircumferenceProtocolId: String, Codable, CaseIterable, Sendable {
  case waistMidpoint
  case hipMaximum
  case custom

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `landmarkCode` (confirmed).
public enum LandmarkCode: String, Codable, CaseIterable, Sendable {
  case tragusLeft
  case tragusRight
  case c7
  case earLeft
  case earRight
  case acromionLeft
  case acromionRight
  case asisLeft
  case asisRight

  public static let vocabStatus: VocabStatus = .confirmed
}

/// vocab `activeOrPassive` (draft, confirmBy DF-916).
public enum ActiveOrPassive: String, Codable, CaseIterable, Sendable {
  case active
  case passive

  public static let vocabStatus: VocabStatus = .draft
}

/// vocab `joint` (draft, confirmBy DF-916).
public enum Joint: String, Codable, CaseIterable, Sendable {
  case cervicalSpine
  case thoracicSpine
  case lumbarSpine
  case shoulder
  case elbow
  case wrist
  case hip
  case knee
  case ankle

  public static let vocabStatus: VocabStatus = .draft
}

/// vocab `motion` (draft, confirmBy DF-916).
public enum Motion: String, Codable, CaseIterable, Sendable {
  case flexion
  case `extension` = "extension"
  case abduction
  case adduction
  case horizontalAbduction
  case horizontalAdduction
  case internalRotation
  case externalRotation
  case lateralFlexion
  case rotation
  case dorsiflexion
  case plantarflexion
  case inversion
  case eversion

  public static let vocabStatus: VocabStatus = .draft
}

/// vocab `muscleGroup` (draft, confirmBy DF-916).
public enum MuscleGroup: String, Codable, CaseIterable, Sendable {
  case neckFlexors
  case neckExtensors
  case shoulderFlexors
  case shoulderAbductors
  case shoulderInternalRotators
  case shoulderExternalRotators
  case elbowFlexors
  case elbowExtensors
  case trunkFlexors
  case trunkExtensors
  case hipFlexors
  case hipExtensors
  case hipAbductors
  case hipAdductors
  case kneeExtensors
  case kneeFlexors
  case ankleDorsiflexors
  case anklePlantarflexors

  public static let vocabStatus: VocabStatus = .draft
}

/// vocab `regionCode` (draft, confirmBy DF-916).
public enum RegionCode: String, Codable, CaseIterable, Sendable {
  case head
  case neck
  case shoulderLeft
  case shoulderRight
  case upperBack
  case lowerBack
  case chest
  case abdomen
  case elbowLeft
  case elbowRight
  case wristHandLeft
  case wristHandRight
  case hipLeft
  case hipRight
  case thighLeft
  case thighRight
  case kneeLeft
  case kneeRight
  case calfLeft
  case calfRight
  case ankleFootLeft
  case ankleFootRight

  public static let vocabStatus: VocabStatus = .draft
}

/// Allowed motions for one joint (vocab `jointMotionPairs`).
public struct JointMotionPair: Sendable, Equatable {
  public let joint: Joint
  public let motions: [Motion]
}

/// vocab `jointMotionPairs` (draft, confirmBy DF-916). While draft the list may be empty and codecs do not check pairs (ASM-P0-04).
public enum JointMotionPairs {
  public static let vocabStatus: VocabStatus = .draft
  public static let pairs: [JointMotionPair] = []
}
